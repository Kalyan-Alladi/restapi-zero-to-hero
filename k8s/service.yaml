apiVersion: v1
kind: Service
metadata:
  name: user-microservice-service
  namespace: user-microservice
  labels:
    app: user-microservice
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: user-microservice